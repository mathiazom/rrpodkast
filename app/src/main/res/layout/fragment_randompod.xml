<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            android:layout_gravity="center"
            android:src="@mipmap/comp"
            android:contentDescription="@string/random_pod_background_description" />

        <!-- <TextView
            android:layout_centerInParent="true"
            android:id="@+id/random_pod_play_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/play_random_background"
            android:gravity="center"
            android:paddingBottom="15dp"
            android:paddingLeft="30dp"
            android:paddingRight="30dp"
            android:paddingTop="15dp"
            android:text="@string/action_play_random_pod"
            android:textColor="@color/app_white"
            android:textSize="17sp" /> -->

    <ImageView
        android:layout_centerInParent="true"
        android:id="@+id/random_pod_play_button"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_gravity="center"
        android:background="@drawable/play_random_background"
        android:gravity="center"
        android:paddingBottom="15dp"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="15dp"
        android:text="@string/action_play_random_pod"
        android:textColor="@color/colorWhite"
        app:srcCompat="@drawable/ic_round_shuffle_24px"
        android:contentDescription="@string/play_random_pod_button_description" />

    </RelativeLayout>